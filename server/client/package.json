{
  "name": "enhanced-coderunner-client",
  "version": "2.0.0",
  "description": "Enhanced CodeRunner client with advanced UI, theming, and customization",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite --host 0.0.0.0 --port 5173",
    "build": "vite build --mode production",
    "preview": "vite preview --host 0.0.0.0 --port 4173",
    "clean": "rm -rf dist node_modules/.cache",
    "build:fallback": "mkdir -p dist && cp index.html dist/ && cp main.js dist/"
  },
  "dependencies": {},
  "devDependencies": {
    "vite": "^5.4.0"
  },
  "optionalDependencies": {
    "@discord/embedded-app-sdk": "^1.0.0"
  },
  "browserslist": [
    "> 1%",
    "last 2 versions",
    "not dead"
  ],
  "engines": {
    "node": ">=18.0.0"
  }
}
